// generated by o3-high-mini
use wrappers::parsers::Parsable;
use serde::{Deserialize, Serialize};
use std::rc::Rc;
use std::any::Any;

#[derive(Default, Debug, Deserialize, Serialize)]
pub struct PollAttrs {
    pub fds: String,
    pub nfds: usize,
    pub timeout: i32,
    pub ret: i32,
}

#[typetag::serde]
impl Parsable for PollAttrs {
    fn parse(args: &str, result: Option<&str>) -> Result<Self, String> {
        let mut attrs = PollAttrs::default();
        let parts: Vec<&str> = args.split(',').map(|s| s.trim()).collect();
        if parts.len() < 3 {
            return Err("Invalid arguments for poll".into());
        }
        attrs.fds = parts[0].to_string();
        attrs.nfds = parts[1].parse().map_err(|e| format!("Failed to parse nfds: {}", e))?;
        attrs.timeout = parts[2].parse().map_err(|e| format!("Failed to parse timeout: {}", e))?;
        if let Some(r) = result {
            attrs.ret = r.trim().parse().unwrap_or(0);
        }
        Ok(attrs)
    }
    
    fn as_any(self: Rc<Self>) -> Rc<dyn Any> {
        self
    }
}
