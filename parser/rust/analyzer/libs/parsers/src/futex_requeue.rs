// generated by o3-high-mini
use wrappers::parsers::Parsable;
use serde::{Deserialize, Serialize};
use std::rc::Rc;
use std::any::Any;

#[derive(Default, Debug, Deserialize, Serialize)]
pub struct FutexRequeueAttrs {
    pub uaddr: String,
    pub val: i32,
    pub uaddr2: String,
    pub val3: i32,
    pub ret: i32,
}

#[typetag::serde]
impl Parsable for FutexRequeueAttrs {
    fn parse(args: &str, result: Option<&str>) -> Result<Self, String> {
        let mut attrs = FutexRequeueAttrs::default();
        let parts: Vec<&str> = args.split(',').map(|s| s.trim()).collect();
        if parts.len() < 4 {
            return Err("Invalid arguments for futex_requeue".into());
        }
        attrs.uaddr = parts[0].to_string();
        attrs.val = parts[1].parse().map_err(|e| format!("Failed to parse val: {}", e))?;
        attrs.uaddr2 = parts[2].to_string();
        attrs.val3 = parts[3].parse().map_err(|e| format!("Failed to parse val3: {}", e))?;
        if let Some(r) = result {
            attrs.ret = r.trim().parse().unwrap_or(0);
        }
        Ok(attrs)
    }
    
    fn as_any(self: Rc<Self>) -> Rc<dyn Any> {
        self
    }
}
